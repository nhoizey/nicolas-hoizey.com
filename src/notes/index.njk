---
layout: default
title: Notes
permalink: "notes/"
eleventyExcludeFromCollections: true
---

{% from "macros/noteCard.njk" import noteCard %}

<h2>Latest notes</h2>
<ul class="list">
  {% for note in collections.notes | limit(10) %}
    <li class="list__item">
      <div class="card card--note h-entry">
        {{ noteCard(note.url, note.lang, note.date, note.data.tags, note.templateContent, webmentions) }}
      </div>
    </li>
  {% endfor %}
</ul>

<h2>Browse all notes</h2>
<p class="pagination">
  Years:
  {% set separator = joiner(", ") %}
  {% for noteYear in collections.yearsWithNotes %}
    {{ separator() }}
    <a href="/notes/{{ noteYear }}/">{{ noteYear }}</a>
  {%- endfor %}
</p>
