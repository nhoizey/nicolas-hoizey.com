---
layout: default
title: Articles
eleventyExcludeFromCollections: true
---

{% from "macros/articleCard.njk" import articleCard %}

<h2>Latest article</h2>
<ul class="list">
  {% for article in collections.articles | limit(1) %}
    <li class="card h-entry">
      {{ articleCard(article) }}
    </li>
  {% endfor %}
</ul>

<h2>Noteworthy articles</h2>
<ul class="list">
  {% for article in collections.promoted %}
    <li class="card h-entry">
      {{ articleCard(article) }}
    </li>
  {% endfor %}
</ul>

<h2>Browse all articles</h2>
<p class="pagination">
  Years:
  {% set separator = joiner(", ") %}
  {% for articleYear in collections.yearsWithArticles %}
    {{ separator() }}
    <a href="/articles/{{ articleYear }}/">{{ articleYear }}</a>
  {%- endfor %}
</p>
