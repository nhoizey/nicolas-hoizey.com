---
layout: default
tags: articles
---

{% set this_url %}{{ site.url }}{{ page.url }}{% endset %}

{#
Breadcrumb JSON-LD:
https://developers.google.com/search/docs/data-types/breadcrumbs
#}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{
    "@type": "ListItem",
    "position": 1,
    "item": {
      "@id": "https://nicolas-hoizey.com/{{ page.date | date("YYYY") }}/",
      "name": "{{ page.date | date("YYYY") }}"
    }
  },{
    "@type": "ListItem",
    "position": 2,
    "item": {
      "@id": "https://nicolas-hoizey.com/{{ page.date | date("YYYY/MM") }}/",
      "name": "{{ page.date | date("MM") }}"
    }
  },{
    "@type": "ListItem",
    "position": 3,
    "item": {
      "@id": "{{ this_url }}",
      "name": "{{ page.title }}"
    }
  }]
}
</script>

<article class="main clearfix h-entry" lang="{{ page.lang }}">
  <h1 id="post" class="card__title p-name">{{ page.title }}</h1>

  {% if page.flickr or page.facebook or page.instagram %}
  {% from "macros/meta.njk" import meta %}
  {{ meta(page.url, page.date, tags) }}

  <div class="e-content">{{ content | safe }}</div>
    <div class="socialphoto">
      <p>You'll also find this photo on your favorite service&nbsp;:
        <span>
        {% if page.flickr %}
          <a href="{{ page.flickr }}" class="icon flickr"><svg><use xlink:href="#symbol-flickr" /></svg></a>
        {% endif %}
        {% if page.facebook %}
          <a href="{{ page.facebook }}" class="icon facebook"><svg><use xlink:href="#symbol-facebook" /></svg></a>
        {% endif %}
        {% if page.instagram %}
          <a href="{{ page.instagram }}" class="icon instagram"><svg><use xlink:href="#symbol-instagram" /></svg></a>
        {% endif %}
        </span>
      </p>
    </div>
  {% endif %}

  <p class="edit">If you want to share an error or suggest an enhancement of this content, please edit <a href="https://github.com/nhoizey/nicolas-hoizey.com/tree/master/{{ page.inputPath }}">the source on Github</a>.</p>

  {% include "components/comments.njk" %}
  {% include "components/webmentions.njk" %}
</article>
